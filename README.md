# cf
centrifugo listener

#Usage:
$ ./cf --help
Usage of ./cf:
  -c string
    	channel name (default "channel")
  -help
    	print help
  -u string
    	url for centrifugo (default "ws://127.0.0.1:8000/connection/websocket")
